git add '*.txt'
git status 
git commit -m 'hi commit'
git log
git remote add origin https://github.com/try-git/try_git.git
git push -u origin master
///The name of our remote is origin and the default local branch name is master. The -u tells Git to remember the parameters, so that next time we can simply run git push and Git will know what to do. Go ahead and push it!
git pull origin master	//pull
git diff HEAD
git diff --staged
git reset octofamily/octodog.txt
git checkout -- octocat.txt
git branch clean_up	//新建一个branch
git checkout ckean_up	//移动到这个branch上
git rm '*.txt'
git commit -m 'Remove all the cats'
git checkout master	//移动到master上
git merge clean_up	//合并clean_up这个branch到当前（master）所在上
git branch -d clean_up	//删掉clean_up这个branch
